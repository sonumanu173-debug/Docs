---
title: "Using and Tuning Predictive Braking"
---

Instead of relying on a manually tuned derivative term to prevent overshoot, this controller predicts how far the robot will slide if it brakes using a small negative voltage. It uses the predicted braking distance to anticipate positional error, effectively treating it as reaction time. This allows the robot to brake precisely when needed, maximizing deceleration and accuracy.

Learn more about Predictive Braking: [link]

# Why use Predictive Braking?
Automatically tunes and maximizes the deceleration speed and accuracy of your robot.

Using this method, the world record auto was achieved, as well as speeding up many other teams' autos.

# Prerequisites
- **Mecanum wheels** (support for swerve and tank coming soon)
- **Odometry Wheels** (drive encoders alone are insufficient due to wheel slip)
- Works best with robots with a balanced center of mass and robots that do not lift off the ground or turn dramatically when braking. Do not worry if your robot does these things. This algorithm will still work. Just consider adding more weight to your robot.

# Tuning
1. Run the Tuning.java OpMode -> Automatic -> PredictiveBrakingTuner. This will give you values for `kQuadratic` and `kLinear`.
In FollowerConstants, add `.predictiveBrakingCoefficents(new PredictiveBrakingCoefficients(kP, kLinear, kQuadratic))`
2. Insert the values given from the tuner into the method. Use a starting kP value around `0.1`.
- `kQuadratic` represents the braking distance proportional to velocity squared. This is caused by constant forces such as sliding friction. 
- `kLinear` represents braking distance proportional to velocity. This is caused by velocity-proportional braking distance such as back-EMF voltage and viscous friction. 
3. Run LineTest and adjust kP to your liking. kP usually ranges from `0.05-0.3`. kP changes are harder to notice and have minimal effects due to kP accounting for predicted error. However, tune kP as high as possible so it will give you the most holding strength and accuracy, but without jittering the robot. 
If you want smoother or sooner deceleration, try experimenting with kP of 0.05 or lower and increasing the kQuadratic term, as this will act more like a motion profile.
4. Currently, it is recommended to turn off centripetal forces in auto, as predictive braking naturally accounts for this.

# Effective Predictive Braking Pathing
For predictive braking to work as fast as possible, continue your PathChain unless it needs to come to a complete stop.
Use curves instead of 90-degree line segments unless you want the robot come to a complete stop. 90-degree line segments will be viable without stopping in future versions when predicting the next path is implemented.

# What does it replace?
In Pedro Pathing, it replaces the previous drive and translational PIDF controllers, using a single unified controller to regulate the robot’s position along the path.
The system employs two controllers: one tangent to the path, where the error is the remaining distance, and one perpendicular to the path, which corrects lateral deviation.
`PredictiveBrakingTuner` automatically measures the robot’s braking behavior and builds a model that enables accurate braking prediction and control.

